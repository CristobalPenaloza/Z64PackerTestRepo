name: Z64 - Release assets test

on:
  workflow_dispatch:

jobs:
  build:
    permissions: write-all
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.x"]

    steps:
      - uses: CristobalPenaloza/z64packer-release-assets@a751d34dc435df106e5782befca8ea8cd408ae33
        with:
          who-to-greet: 'Mona the Octocat'
