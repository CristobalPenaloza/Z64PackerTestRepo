name: Z64 - Release assets test

on:
  workflow_dispatch:

jobs:
  build:
    permissions: write-all
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.x"]

    steps:
      - uses: CristobalPenaloza/z64packer-release-assets@2d74899811a82f1a265e97c092fd6f77a8156ff3
        with:
          who-to-greet: 'Mona the Octocat'
