name: Z64 - Release assets test

on:
  workflow_dispatch:

jobs:
  build:
    permissions: write-all
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.x"]

    steps:
      - uses: CristobalPenaloza/z64packer-release-assets@d87204614f550d8ad61ad4a54e3c4d378ec0c7c5
        with:
          who-to-greet: 'Mona the Octocat'
